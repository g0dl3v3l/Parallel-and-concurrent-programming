Bakery Lock Execution:
    Compile the Bakery Lock program:
        g++ -std=c++17 -pthread bakeryLock.cpp -o bakery
    Run the compiled executable:
        ./bakery

Filter Lock Execution:

    Compile the Filter Lock program:
        g++ -std=c++17 -pthread filterLock.cpp -o filter
    Run the compiled executable:
        ./filter
